version: '3.8'

services:
  warungpos-landing:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: warungpos-landing
    restart: unless-stopped
    ports:
      - "${PORT:-8082}:80"
    volumes:
      - .:/usr/share/nginx/html
    environment:
      - NGINX_PORT=80
